<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<p><%= link_to "Edit this task", edittask_path %></p>
<p><%= link_to "Back to tasks", tasks_path %></p>

<%#= link_to "back to tasks", tasks_path %>
<!-- <p><%= @task %></p> -->



<!-- last line is the object id returned from the controller. #here on html view page we get all object from show action inside controller and then from the object we pick the value we want to show to the user -->
